1. Para instalar json-server:

npm install -g json-server

2. Para ejecutar json-server:

json-server --watch _JSON-SERVER/publicaciones.json

json-server --watch _JSON-SERVER/publicaciones.json --host localhost --port 3000

json-server --watch _JSON-SERVER/publicaciones.json --host 192.168.95.128 --port 3000

